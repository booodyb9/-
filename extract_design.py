import re
import json
import sys

# We don't have the raw message file in the workspace easily accessible unless we copy it.
# I will just write the CSS and HTML directly since I have it in the prompt.
