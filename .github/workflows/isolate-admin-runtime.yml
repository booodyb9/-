name: Isolate admin runtime

on:
  push:
    branches:
      - agent/project-device-uploads

permissions:
  contents: write

jobs:
  optimize-and-validate:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/project-device-uploads
          fetch-depth: 0

      - name: Apply admin runtime isolation
        run: python scripts/isolate-admin-runtime.py

      - name: Remove temporary migration files
        run: |
          rm -f scripts/isolate-admin-runtime.py
          rm -f .github/workflows/isolate-admin-runtime.yml

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci --legacy-peer-deps --no-audit --no-fund

      - name: TypeScript
        run: npm run lint

      - name: Production build validation
        env:
          VITE_SUPABASE_URL: https://example.supabase.co
          VITE_SUPABASE_ANON_KEY: ci-placeholder-key
        run: npm run build

      - name: Commit validated optimization
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "perf: isolate admin network work from public pages"
          git push origin HEAD:agent/project-device-uploads
